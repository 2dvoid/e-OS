# --- Layout ---
width=400
height=360
# location options: center, top, bottom, top_right, etc.
location=center

# --- Behavior ---
show=drun
prompt=Search...
filter_rate=100
allow_markup=true
no_actions=true
halign=fill
orientation=vertical
content_halign=fill
insensitive=true
allow_images=true
image_size=24

# --- Terminal ---
# Important for "run" mode or shift+enter actions
term=kitty

# --- Hide Scrollbars ---
# Keeps the UI clean
hide_scroll=true
